{
  "name": "morpho-blue-rewards",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ morpho-org/morpho-blue-rewards",
    "create-local": "graph create --node http://localhost:8020/ morpho-org/morpho-blue-rewards",
    "remove-local": "graph remove --node http://localhost:8020/ morpho-org/morpho-blue-rewards",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 morpho-org/morpho-blue-rewards",
    "test": "graph test",
    "lint": "prettier --check src tests",
    "lint:fix":  "prettier --write src tests"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "@trivago/prettier-plugin-sort-imports": "^4.2.0",
    "prettier": "^3.0.3"
  },
  "prettier": {
    "printWidth": 120,
    "plugins": [
      "@trivago/prettier-plugin-sort-imports"
    ],
    "importOrder": [
      "^@",
      "^\\.\\.",
      "^\\."
    ],
    "importOrderSeparation": true
  }
}
